import java.awt.image.BufferedImage;

public class ManageTheLevels {
    private Game game;
    private BufferedImage[] spritePlaces;

    public ManageTheLevels(Game game, BufferedImage[] spritePlaces) {
        this.game = game;
       // this.spritePlaces = spritePlaces;
    }
   /* public void importSprites(){
        BufferedImage current =
        spritePlaces = new BufferedImage[16];
        for(int x = 0; x < 4; x++)
            for(int y = 0; y < 4; y++){
                int index = x*4 + y;
            }

    }*/
}
